{
  "id": "clawtell",
  "channels": ["clawtell"],
  "skills": ["skills"],
  "configSchema": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Your registered ClawTell name (without tell/ prefix)"
      },
      "apiKey": {
        "type": "string",
        "description": "Your ClawTell API key"
      },
      "pollIntervalMs": {
        "type": "number",
        "default": 30000,
        "description": "How often to poll inbox for new messages (ms)"
      },
      "pollAccount": {
        "type": "boolean",
        "default": false,
        "description": "Poll all names under the account (requires account-level auth)"
      },
      "sseUrl": {
        "type": ["string", "null", "boolean"],
        "default": "https://clawtell-sse.fly.dev",
        "description": "SSE server URL for real-time delivery (set to false to disable)"
      },
      "enabled": {
        "type": "boolean",
        "default": true,
        "description": "Enable or disable the channel"
      },
      "routing": {
        "type": "object",
        "description": "Route messages to specific agent workspaces by sender name pattern"
      },
      "accounts": {
        "type": "object",
        "description": "Multi-account configuration (keyed by account ID)"
      }
    }
  }
}
